---
batch_connect:
  template: "basic"
  conn_params:
    -  csrf_token

# TODO remove this when we move to the gizmo cluster (actually change the partition to `interactive`)
script:
  native: [
    "-p", "vnc",
  <% if want_gpu == "1" %>
    "--gres=gpu"
  <% end %>
  ]